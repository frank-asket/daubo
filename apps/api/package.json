{
  "name": "@daubo/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "python3 -m compileall app",
    "lint": "python3 -m compileall app",
    "alembic:upgrade": "python3 -m alembic upgrade head"
  }
}
